# Force pnpm to flatten node_modules so Metro, Expo, and Native tools find everything
public-hoist-pattern[]=*

# Suppress strict peer dependency crashes for older packages adapting to React 19
strict-peer-dependencies=false

# Automatically handle your existing dependency overrides structure
auto-install-peers=true
